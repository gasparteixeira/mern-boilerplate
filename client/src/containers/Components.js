import React from "react";
import { H1 } from "../components/Heading";
import { Container, Row, Col } from "reactstrap";

const Components = () => (
    <Container fluid>
        <H1 text="My components" />
        <p>That's the components container</p>
    </Container>
)

export default Components;